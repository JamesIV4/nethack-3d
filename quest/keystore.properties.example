# Optional Quest-specific release key. Copy to keystore.properties (ignored).
# Otherwise the build reuses android/keystore.properties.
# Relative storeFile paths resolve from this properties file's directory.
storeFile=../android/your-release-key.jks
storePassword=
keyAlias=
keyPassword=
